import { SelectedMoviePipe } from './selected-movie.pipe';

describe('SelectedMoviePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
